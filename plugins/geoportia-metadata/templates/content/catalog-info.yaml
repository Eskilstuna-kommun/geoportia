apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name | dump }}
  annotations:
    geoportia.se/metadata-schema: ${{ values.metadata.schema | dump }}
    geoportia.se/metadata: ${{ values.metadata.metadata | dump }}
spec:
  type: service
  owner: user:guest
  lifecycle: experimental
